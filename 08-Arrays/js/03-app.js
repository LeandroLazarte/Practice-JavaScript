const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto'];
console.table(meses);

// Recorriendo un arreglo.
console.log(meses.length);


// For loop recorre el arreglo y muestra los valores de cada posicion
for (let i = 0; i < meses.length; i++) {
    // console.log(meses[i]);
}

const marcas = ['Apple', 'Samsung', 'Xiaomi', 'LG', 'Corsair'];

for (let i = 0; i < marcas.length; i++) {
    // console.log(marcas[i]);
}

const verduras = ['Zanahoria', 'Tomate', 'Zapallo', 'Chaucha', 'Pepino'];
for (let i = 0; i < verduras.length; i++) {
    console.log(verduras[i]);
}