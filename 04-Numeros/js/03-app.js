let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.2);
resultado = Math.round(2.3);
resultado = Math.round(2.5); // Mayor o igual a .5 redondea hacia arriba
resultado = Math.round(2.8);

// Redondear hacia arriba
resultado = Math.ceil(2.1);


// Redondear hacia abajo
resultado = Math.floor(2.9);

// Raiz cuadrada
resultado = Math.sqrt(144);
resultado = Math.sqrt(121);

// Valor absoluto
resultado = Math.abs(-1000);

// Potencia
resultado = Math.pow(2 ,4);
resultado = Math.pow(2 ,8);

// Minimo
resultado = Math.min(1, 2, 6 ,3);

// Max
resultado = Math.max(1, 2, 6 ,3);

// Aleatorio
// resultado = Math.random() * 12;

// Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 20);


console.log(resultado);